# Example environment file for tsbridge systemd service
# Copy this to /etc/tsbridge/tsbridge.env and update with your values

# Tailscale OAuth credentials (if not in config file)
# TS_OAUTH_CLIENT_ID=your-client-id
# TS_OAUTH_CLIENT_SECRET=your-client-secret

# Or use auth key for ephemeral nodes
# TS_AUTHKEY=your-auth-key

# HTTP/HTTPS proxy settings (if needed)
# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1,.internal.domain

# Custom GOMAXPROCS (defaults to 0 = all CPUs)
# GOMAXPROCS=4

# Enable debug logging (set any non-empty value)
# TSBRIDGE_DEBUG=1